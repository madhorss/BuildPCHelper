<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.best.buildpc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/a"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.upgradeResult"></activity>
        <activity android:name=".Activity.upgradePart" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.generate" /> <!-- activity android:name=".scratch" / -->
        <activity android:name=".Activity.upgrade" />
        <activity android:name=".Activity.collection" />
        <activity android:name=".Activity.saved" />
        <activity android:name=".Activity.Scratch" />
        <activity android:name=".Activity.choose" />
        <activity android:name=".Activity.detail" />
        <activity android:name=".Activity.preview" />
    </application>

</manifest>